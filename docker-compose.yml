services:
  echo-journal:
    build: .
    ports:
      - "8510:8000"
    volumes:
      - ./static:/app/static
      - ./prompts.json:/app/prompts.json
      - ${JOURNALS_DIR:-/mnt/nas/journals}:/journals
    environment:
      - TZ=America/New_York
